el proyecto consiste en hacer lo siguentes puntos 
Desarrollar las API's:
post: /products: debe utilizar la función readJson desarrollada en clase, el objetivo es que a traves de este endpoint pueda insertar en el archivo .json
put: /products: debe utilizar la función readJson desarrollada en clase, el objetivo es que a traves de este endpoint pueda actualizar en el archivo .json
delete: /products: debe utilizar la función readJson desarrollada en clase, el objetivo es que a traves de este endpoint pueda eliminar registros en el archivo .json

lo cual se implementaron y probaron 